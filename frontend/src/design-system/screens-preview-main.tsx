import React from "react";
import ReactDOM from "react-dom/client";
import { ScreensPreviewStandalone } from "@/design-system/ScreensPreviewStandalone";
import "@/index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <ScreensPreviewStandalone />
  </React.StrictMode>
);
